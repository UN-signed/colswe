<%= form_with(model: research_area, local: true) do |form| %>
  <% if research_area.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(research_area.errors.count, "error") %> prohibited this research_area from being saved:</h2>

      <ul>
      <% research_area.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :research_area_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :research_area_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
