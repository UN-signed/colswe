<div class="dashboard">
  <div class="ui secondary pointing menu" id="dashboard-menu">
    <a class="item active" data-items="projects, .articles">Todos</a>
    <a class="item" data-items="projects">Proyectos</a>
    <a class="item" data-items="articles">Artículos</a>
    <div class="right menu">
      <div class="ui transparent icon input">
        <input type="text" placeholder="Buscar...">
        <i class="search link icon"></i>
      </div>
    </div>
  </div>

  <div class="holder articles">
    <div class="ui three cards">
    <%= render @projectsAndArticles %>
    </div>
    <br>
    <br>

    <%= will_paginate @projectsAndArticles, previous_label:'Anterior', next_label:'Siguiente', renderer: WillPaginateSemanticUi::ActionView::Renderer %>

  </div>

  <!-- <div class="holder projects">
    <div class="ui three cards">
      <%= render @projects %>
    </div>
    <br>
    <br>

    <= will_paginate @projects, :param_name => 'project_page', previous_label:'Anterior', next_label:'Siguiente', renderer: WillPaginateSemanticUi::ActionView::Renderer %>
  </div> -->
</div>
