<div class="card">
  <div class="image">
    <% user = User.find(member.user_id) %>
    <%= image_tag(user.photo.url, alt: 'Avatar') %>
  </div>
  <div class="content">
    <a class="header">
      <%= user.name %>
    </a>
    <div class="meta">
      <span class="date">Miembro desde el <%= format_time(user.created_at, :short, "N/A") %></span>
    </div>
  </div>
  <div class="extra content">
  </div>
</div>
